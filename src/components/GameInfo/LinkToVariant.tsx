export const LinkToVariant = ({ value }: { value: string }) => (
  <>
    <a href={value} class={'h-7 flex items-center bg-primary px-8 rounded text-white font-semibold text-[14px]'}>
      Try this variant
    </a>
  </>
)
